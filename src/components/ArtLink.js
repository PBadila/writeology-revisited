const ArtLink = ({tag,title}) =>{
    return (
        <div class='artLink'>
            <h3 class='artTag'>{tag}</h3>
            <div class='artTitle'>{title}</div>
        </div>
    )
}

export default ArtLink